
import T "../data-types/types";
import Base "mo:waterway-mops/BaseTypes";

module ShotQueries {

    public type GetShot = {
        principalId: Base.PrincipalId;
    };

    public type Shot = {

    };

    public type PredictShot = {
        principalId: Base.PrincipalId;

    };

    public type PredictedShot = {

    };

}

  